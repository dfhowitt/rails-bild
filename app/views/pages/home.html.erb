<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)),
url('<%= image_path "banner.jpg", height: 400, crop: :fill %>'); background-size: cover; background-position: 50% 60%;">
  <div class="title">
    <h1>Bild</h1>
    <h4>Connecting you with construction work</h4>
  </div>

  <form method="get" action="/projects" class="w-50 p-3">
    <input type="text" name="query" autocomplete="off" placeholder="Enter Postcode" tabindex="100" class="form-control" id="flat_address">
    <a class="btn btn-flat" href="#">Search</a>
  </form>
</div>

<div class="container">
  <h2>How It Works</h2>
  <div class="cards mt-5">
    <div class="card-category" style="width: 22rem;">
      <div class="card-body">
        <i class="fas fa-map-marked-alt"></i>
        <h3 class="card-title">Find</h3>
        <p class="card-text">We give you access to every registered building site on our database, which you can filter based on location, pay, project duration and more.</p>
      </div>
    </div>
    <div class="card-category" style="width: 22rem;">
      <div class="card-body">
        <i class="fas fa-check"></i>
        <h3 class="card-title">Confirm</h3>
        <p class="card-text">Confirm with a click of a button. Some sites will require an application to prove you have the necessary skills.</p>
        </div>
    </div>
    <div class="card-category" style="width: 22rem;">
      <div class="card-body">
        <i class="far fa-comments"></i>
        <h3 class="card-title">Messaging</h3>
        <p class="card-text">Bild has an in-built multi purpose messaging platform that will keep you up to date once you're job is confirmed.</p>
      </div>
    </div>
  </div>
</div>

<h2>Recently Added Jobs</h2>

<div class="home-projects-grid mx-4">
    <%@projects.each do |project|%>
      <%= link_to project_path(project) do %>
        <div class="homepage-project-card" style="background-image: linear-gradient(rgba(0,0,0,0), rgba(0,0,0,0.2)), url('<%= cl_image_path project.site.photo.key, height: 300, width: 400, crop: :fill %>')">
          <% if site.project.photo.attached? %>
            <div class="homepage-project-card-info h-100">
              <div class="bottom text-right px-2 py-1">
                <h5 class="mb-1"><%= project.job_type %></h5>
                <p class="mb-1"><%= project.capacity %> Needed · <%= project.wage %> · <%=project.start_date %> - <%=project.end_date %> </p>
                <p class="mt-1"><%= project.description %></p>
                <p><%= project.site.location %></p>
              </div>
            </div>
          <% else %>
            <div class="homepage-project-card-photo" style="background-color: $light-gray"></div>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>


