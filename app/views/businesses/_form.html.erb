<%= simple_form_for(business) do |f| %>
    <%= f.input :name, input_html: { placeholder: "Enter a name for your business" } %>
    <%= f.submit "Create Business", class: "btn btn-primary w-100"%>
<% end %>
